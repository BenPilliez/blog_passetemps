<nav class="navbar navbar-expand-lg navbar-light fixed-top py-3" id="mainNav">
	<a class="navbar-brand " href="{{path('index')}}">Mon Blog</a>
	<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#nav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
		Menu
		<span class="navbar-toggler-icon"></span>
	</button>
	<div class="collapse navbar-collapse" id="nav">
		<ul class="navbar-nav white m-0 ml-lg-auto p-3 p-lg-0">
			<li class="d-inline d-lg-none">
				<button data-toggle="collapse" data-target="#nav" class="btn close float-right">&times;</button>
			</li>
			{% for category in categories  %}
				<li class="nav-item ">
					<a class="nav-link text-truncate" href="{{path('post.index', {id: category.id})}}">{{category.name}}</a>
				</li>
			{% endfor %}
			<li class="nav-item ">
				<a class="nav-link text-truncate" href="{{path('about')}}">A propos</a>
			</li>
		</ul>
	</div>
</nav>
