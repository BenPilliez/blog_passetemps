{% for post in posts  %}
	<li>
		<figure>
			<img src="{{vich_uploader_asset(post, 'thumbnailFile') | imagine_filter('medium')}}" alt="image-{{post.title}}">
			<figcaption>
				<h3>{{post.title}}</h3>
				<a href="{{path('post.show', {id: post.id, slug: post.slug})}}">Voir l'article</a>
			</figcaption>
		</figure>
	</li>
{% endfor %}
